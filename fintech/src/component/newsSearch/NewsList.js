import React from 'react'

const NewsList = ({searchResult}) => {
    return (
        <div>
            {searchResult.map((news) =>{
                return ();
            })}
        </div>
    )
}

export default NewsList
